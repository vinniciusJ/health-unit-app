import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    list: {
        display: 'flex',
        rowGap: 24,
    },
    listItem: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'center',
        gap: 12,
        marginBottom: 24
    }
})
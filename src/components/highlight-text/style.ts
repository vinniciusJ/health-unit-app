import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        display: 'flex',
        flexDirection: 'row'
    },
    highlight: {
        fontWeight: '800',
    },
    normal: {
        fontWeight: '400'
    }
})
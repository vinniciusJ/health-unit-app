import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    goBack: {
       gap: 16,
       justifyContent: 'flex-start',
       marginBottom: 24
    },
    goBackTitle: {
        fontSize: 18
    }
})